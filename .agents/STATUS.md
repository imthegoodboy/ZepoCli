# ZepoCli Current Status

Last updated: 2026-06-07 16:43 IST.

## Local Package State

- Local release gates passed on 2026-06-07 at 16:43 IST after making checkout `--wait` live-command timeout reports keep the fixed Zepto handoff recovery markers in `error.hint`: `Payment link: https://www.zepto.com/?cart=open` and `Payment link session: user_zepto_session_required`. Generic timeouts still keep the existing `--step-timeout` hint, and checkout wait timeouts still leave checkout/track coverage missing instead of becoming production-scope proof. Verified: focused live-verifier/README/agent-guidance/package-contract tests, `npm run build`, full `npm test` with 34 files and 697 tests, `npm run verify:cli`, `npm run verify:package`, `npm run verify:secrets`, `npm run verify:dependencies`, `npm run verify:audit` with 0 vulnerabilities, `npm pack --dry-run`, `npm run verify:publish-dry-run`, `node dist/index.js --help`, and `git diff --check` with only CRLF normalization warnings for `scripts/verify-package.mjs` and `tests/live-verifier.test.ts`.
- Local release gates passed on 2026-06-07 at 16:28 IST after making `verify:live --checkout-wait` print the fixed Zepto payment link/session markers before launching the waiting checkout child, so a later human-prompt timeout still leaves `Payment link: https://www.zepto.com/?cart=open` and `Payment link session: user_zepto_session_required` in the runner console. This does not automate payment and does not make timeout/manual checkout evidence count as production-scope checkout handoff coverage. Verified: focused live-verifier/README/agent-guidance/package-contract tests, `npm run build`, full `npm test` with 34 files and 697 tests, `npm run verify:cli`, `npm run verify:package`, `npm run verify:secrets`, `npm run verify:dependencies`, `npm run verify:audit` with 0 vulnerabilities, `npm pack --dry-run`, `npm run verify:publish-dry-run`, `node dist/index.js --help`, and `git diff --check` with only CRLF normalization warnings for `scripts/verify-package.mjs` and `tests/live-verifier.test.ts`.
- Local release gates passed on 2026-06-07 at 16:12 IST after making `zepo --visible checkout --wait` / JSON wait mode print `Payment link: https://www.zepto.com/?cart=open` and `Payment link session: user_zepto_session_required` to stderr before the human Enter prompt. This gives humans and agents the Zepto-owned session-bound checkout/payment link even when the prompt later times out, while keeping stdout JSON clean and still not clicking payment/order controls. Verified: focused handoff-service/README/agent-guidance/package-contract tests, `npm run build`, full `npm test` with 34 files and 697 tests, `npm run verify:cli`, `npm run verify:package`, `npm run verify:dependencies`, `npm run verify:audit` with 0 vulnerabilities, `npm pack --dry-run`, and `npm run verify:publish-dry-run`.
- `npm run check` passed locally on 2026-06-07 at 15:40 IST with the current `main` worktree. Secret scan, dependency readiness, build, full Vitest suite with 34 files and 696 tests, compiled CLI smoke, installed-package smoke, help output, audit with 0 vulnerabilities, pack dry-run, and publish dry-run all passed. The installed-package verifier still covers the checkout/payment handoff contract, session-bound payment-link marker, manual checkout boundary guidance, background automation guards, redaction, live report validation, command help, completion scripts, and browser diagnostics.
- Local gates passed on 2026-06-07 at 15:07 IST after adding safe `verify:live` console guidance; this status note was refreshed at 15:27 IST after pinning the manual checkout payment-link handoff in `verify:live --help`. When checkout returns `checkout_manual_action_required`, the live runner now prints `Payment link: https://www.zepto.com/?cart=open` and `Payment link session: user_zepto_session_required` so the human/operator can open the Zepto-owned checkout/payment link in the user's Zepto session without reading the saved report first; the help output also states this is handoff guidance only, not payment proof or order proof. ZepoCli still does not click final payment/order controls and still does not treat the link as payment proof or order proof. Verified before the status refresh: focused live-verifier/README/agent-guidance/package-contract tests, `npm run build`, `npm run verify:package`, `npm ci --include=prod --include=dev` to restore the local dependency tree after a missing-dependency gate failure, `npm run verify:dependencies`, `npm run verify:secrets`, `npm run verify:cli`, full `npm test` with 34 files and 696 tests, and `git diff --check`. Verified after the status refresh: `npm run verify:secrets`, focused agent-guidance test, and `git diff --check`.
- Local gates passed on 2026-06-06 at 15:22 IST after carrying the session-bound Zepto checkout/payment link marker into sanitized live-verification reports. Checkout step summaries and diagnostic manual checkout evidence now preserve and validate `paymentLinkSession: "user_zepto_session_required"` alongside `paymentLink: "https://www.zepto.com/?cart=open"`, so saved reports cannot imply a generic payment-provider URL or payment proof. `verify:live:report --help`, README guidance, `.agents` guidance, source tests, and installed-package verification pin that report contract. Verified: focused live-verifier/agent-guidance/README/package-contract tests, `npm run build`, `npm run verify:cli`, `npm run verify:package`, `npm run verify:secrets`, `npm run verify:dependencies`, `git diff --check`, and full `npm test` with 34 files and 696 tests.
- Local release gates passed individually on 2026-06-06 at 14:58 IST after making the Zepto checkout/payment link explicitly session-bound. Checkout JSON and non-empty cart JSON now include `paymentLinkSession: "user_zepto_session_required"` alongside `paymentLink: "https://www.zepto.com/?cart=open"`, and non-empty human cart/checkout output tells users to open the link in the user's Zepto session while Zepto handles payment. ZepoCli still does not click final payment/order controls and still keeps `paymentStatus: "not_observed_by_zepocli"` plus `orderPlacement: "not_confirmed_by_zepocli"`. Verified: `npm run verify:secrets`, `npm run verify:dependencies`, `npm run build`, focused output/checkout/README/agent-guidance/package-contract/live-verifier tests, full `npm test` with 34 files and 696 tests, `npm run verify:cli`, `npm run verify:package`, `node dist/index.js --help`, `npm audit --omit=dev`, `npm pack --dry-run`, and `npm publish --dry-run --access public`.
- `npm run check` passed locally on 2026-06-06 at 14:30 IST after adding human `zepo cart` checkout/payment handoff guidance. Non-empty human cart output now prints `Checkout: zepo --visible checkout` and `Payment link: https://www.zepto.com/?cart=open` after the cart total, while empty cart output omits payment handoff guidance. The compiled verifier now reports `pass cart public checkout metadata contract`, and installed-package verification still reports `pass installed public output contract`, proving the plain human cart footer and JSON cart metadata both survive `dist` and packed npm installation. Secret scan, dependency readiness, build, 34 test files, 696 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed. Focused output/package-contract/README/agent-guidance tests, `npm run verify:secrets`, `npm run build`, `npm run verify:cli`, and `npm run verify:package` also passed before the full gate.
- `npm run check` passed locally on 2026-06-06 at 14:05 IST after pinning the non-empty cart JSON checkout handoff metadata in compiled and installed-package verification. `npm run verify:cli` now includes `pass cart JSON checkout metadata contract`, and `npm run verify:package` now includes `pass installed public output contract`, proving the compiled `dist` and packed npm install both emit `checkout.command`, `checkout.waitCommand`, and `checkout.paymentLink` for non-empty cart JSON while omitting checkout metadata for empty carts and keeping raw cart text internal. Secret scan, dependency readiness, build, 34 test files, 694 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed. Focused output/package-contract/README/agent-guidance tests, `npm run verify:secrets`, `npm run build`, `npm run verify:cli`, and `npm run verify:package` also passed before the full gate.
- `npm run check` passed locally on 2026-06-06 at 13:41 IST after adding non-empty cart JSON checkout handoff metadata. `zepo cart --json` now keeps raw Zepto page text internal while adding `checkout.command: "zepo --visible checkout"`, `checkout.waitCommand: "zepo --visible checkout --wait"`, and `checkout.paymentLink: "https://www.zepto.com/?cart=open"` with human-action and payment/order-unconfirmed markers, so agents can hand the user the Zepto-owned checkout/payment link or run visible checkout without guessing a `/cart` route. Checkout JSON still uses the same shared `paymentLink`/`handoffUrl` marker and ZepoCli still does not click payment/order controls. Secret scan, dependency readiness, build, 34 test files, 694 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed. Focused output/checkout/README/agent-guidance tests, `npm run verify:secrets`, `npm run build`, `npm run verify:cli`, and `npm run verify:package` also passed before the full gate.
- `npm run check` passed locally on 2026-06-06 at 13:05 IST after adding the checkout JSON/report `paymentLink: "https://www.zepto.com/?cart=open"` alias alongside `handoffUrl` and `paymentHandoffUrl`. The alias is the user-facing Zepto-owned payment/checkout link a user or agent can navigate to, while `paymentStatus: "not_observed_by_zepocli"` and `orderPlacement: "not_confirmed_by_zepocli"` remain unchanged and ZepoCli still does not click payment/order controls. Secret scan, dependency readiness, build, 34 test files, 692 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed. A focused precheck also passed `npm run verify:secrets`, checkout/live-verifier/README/agent-guidance/package-contract tests, `npm run build`, `npm run verify:cli`, and `npm run verify:package`.
- Safe no-account live-verifier smoke ran locally on 2026-06-06 at 12:33 IST with `npm --silent run verify:live -- --data-dir ./.zepo-live-smoke-current --report ./.zepo-live-smoke-current/live-verification-report.json`. The runner passed `doctor` and local `status`, then stopped at the expected `session precondition` with `live_verification_incomplete` because the disposable data directory has no Zepto login. `verify:live:report` rejected the saved report with `live_report_not_ok`, as expected; this proves the current runner/report schema writes sanitized incomplete evidence without treating no-account preflight as production readiness.
- `npm run check` passed locally on 2026-06-06 at 12:29 IST after propagating the fixed Zepto checkout/payment handoff link into sanitized live-verification checkout summaries and manual evidence. `paymentHandoffUrl: "https://www.zepto.com/?cart=open"` is now present in checkout JSON, live-report checkout summaries, live-report manual checkout evidence, README, verifier help, package verifier contracts, and `.agents` guidance while `paymentStatus: "not_observed_by_zepocli"` and `orderPlacement: "not_confirmed_by_zepocli"` still keep payment/order placement unclaimed. Secret scan, dependency readiness, build, 34 test files, 692 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- Local release checks passed on 2026-06-06 at 12:02 IST after adding the checkout JSON `paymentHandoffUrl: "https://www.zepto.com/?cart=open"` contract. The field is a Zepto-owned payment/checkout handoff link for the user or agent to navigate to, while `paymentStatus: "not_observed_by_zepocli"` and `orderPlacement: "not_confirmed_by_zepocli"` remain unchanged and ZepoCli still does not click payment/order controls. Verified: `npm run verify:secrets`, `npm run verify:dependencies`, `npm run build`, focused checkout/README/agent-guidance/package-contract tests, full `npm test` with 34 files and 692 tests, `npm run verify:cli`, `npm run verify:package`, `node dist/index.js --help`, `npm audit --omit=dev`, `npm pack --dry-run`, and `npm publish --dry-run --access public`.
- `npm run check` passed locally on 2026-06-06 at 11:24 IST with the current `main` worktree. Secret scan, dependency readiness, build, 34 test files, 692 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed. A focused precheck also passed `npm run verify:secrets` and `npm test -- --run tests/cart-automation.test.ts tests/checkout.test.ts tests/live-verifier.test.ts`, covering the current Zepto cart drawer shape with out-of-stock/alternative panels, item-limit removal boundaries, manual amount-bearing `Click to Pay`/`Pay` controls, and live-report checkout boundary contracts.
- `npm run check` passed locally on 2026-06-06 at 10:57 IST after aligning `.agents` guidance with the live runner's diagnostic manual checkout boundary wording. Future-agent README, builder skill, and live-verifier skill now state that valid `checkout_manual_action_required` evidence may set diagnostic `checkoutManualBoundary` coverage, but it remains manual Zepto continuation evidence and does not satisfy checkout handoff, payment proof, order-placement proof, or production-scope readiness. Focused agent-guidance/readme/package-contract tests also passed. Secret scan, dependency readiness, build, 34 test files, 692 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 10:35 IST after adding explicit `verify:live --help` guidance that valid `checkout_manual_action_required` evidence may set diagnostic `checkoutManualBoundary` coverage but still does not satisfy checkout handoff, payment proof, order-placement proof, or production-scope readiness. Source live-verifier tests and installed-package verification pin the runner-help contract. Focused live-verifier/package-contract tests also passed. Secret scan, dependency readiness, build, 34 test files, 692 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 09:07 IST after adding explicit live-verifier help and installed-package contract coverage for diagnostic `coverage.checkoutManualBoundary`. The help now states that the flag can be true only for valid sanitized manual checkout evidence and does not satisfy checkout handoff, payment proof, order-placement proof, or production-scope readiness. Focused live-verifier/package-contract tests also passed. Secret scan, dependency readiness, build, 34 test files, 692 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 08:21 IST after adding diagnostic `checkoutManualBoundary` capability reporting for valid sanitized manual checkout evidence. Failed checkout steps that stop at Zepto's human-only cart-side payment-control boundary can now set `attempted.checkoutManualBoundary` and `coverage.checkoutManualBoundary` without satisfying `coverage.checkoutHandoff`, payment proof, order-placement proof, or production-scope readiness. Source tests, installed-package verification, README, and `.agents` guidance pin the contract. Secret scan, dependency readiness, build, 34 test files, 692 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 07:32 IST after making remove mutations expose explicit clicked-row evidence. `zepo remove --json` now returns `removedItems` with readable price or unit detail plus the resulting `cart`, and the live-report validator requires the same removed-item evidence before focused remove coverage can pass. Source tests, installed-package verification, README, and `.agents` guidance pin the stricter contract. Secret scan, dependency readiness, build, 34 test files, 692 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 06:41 IST after tightening focused history live-report coverage. `zepo history` can still return an empty list when Zepto explicitly shows no orders, but `verify:live` history coverage now requires at least one readable order record with status or ETA, so an empty JSON array cannot be reused as proof of order-history parsing. Source tests, installed-package verification, README, and `.agents` guidance pin the stricter contract. Secret scan, dependency readiness, build, 34 test files, 688 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 06:18 IST after removing the stale checkout-service fallback that could return checkout handoff output without sanitized cart evidence. `CheckoutService` now relies on `openCheckout` as the cart-precondition authority, and the focused service regression proves wait-mode re-detection can change the handoff mode while preserving `cartEvidence` from the original readable cart. Secret scan, dependency readiness, build, 34 test files, 688 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 05:59 IST after threading sanitized checkout cart evidence through live-verification reports. Checkout summaries and diagnostic manualEvidence now preserve `manualPaymentControlVisible`, `checkoutCartItemCount`, and `checkoutHasPayableTotal`; accepted checkout handoff coverage requires a positive sanitized cart count, payable-total evidence, and no visible manual payment control, while manual-only cart payment states remain `live_verification_incomplete`. Source tests, installed-package verification, README, verifier help, and `.agents` guidance pin the stricter report contract. Secret scan, dependency readiness, build, 34 test files, 688 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 05:25 IST after adding sanitized checkout precondition evidence to checkout JSON. Runtime checkout handoff results now carry `manualPaymentControlVisible` and optional `cartEvidence` with item count plus payable-total presence, so agents can distinguish a visible human-only cart-side payment control from a missing checkout footer without exposing raw item names, raw page text, payment values, or clicking payment/order controls. Source tests, compiled CLI verification, installed-package verification, README, and `.agents` guidance pin the contract. Secret scan, dependency readiness, build, 34 test files, 688 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 04:52 IST after tightening final production-scope live-report cart evidence. The report validator now rejects production-scope reports whose cart step has readable rows but no total/payable evidence, while focused cart reports can still prove readable cart rows without requiring a total. Source tests, installed-package verification, README, verifier help, and `.agents` guidance pin the stricter final-readiness contract. Secret scan, dependency readiness, build, 34 test files, 687 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 04:23 IST after requiring account-workflow live reports to preserve visible `doctor`/`status` preflight command evidence when `liveSession` is requested. The validator now rejects edited reports that hide visible preflight behind background doctor/status commands, so headless cooldown/access-challenge state cannot be mistaken for human-controlled live proof. Source tests, installed-package verification, README, and `.agents` guidance pin the stricter contract. Secret scan, dependency readiness, build, 34 test files, 687 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 03:57 IST after tightening saved live-report local-status summaries. The report validator now requires passing local `status` steps to include the runner-written `liveSessionState` summary, so edited reports cannot strip that session-state evidence while still being accepted. Source tests, installed-package verification, README, and `.agents` guidance pin the stricter contract. Secret scan, dependency readiness, build, 34 test files, 687 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 03:32 IST after treating cart-side amount-bearing `Continue to Payment ₹...` and `Proceed to Payment ₹...` controls as human-only manual checkout boundaries. The checkout matcher now returns manual-action evidence for those variants instead of falling through as checkout unavailable, while still refusing to click payment/order controls. README, `.agents` guidance, source tests, and installed-package contract checks pin the behavior. Secret scan, dependency readiness, build, 34 test files, 687 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 03:10 IST after updating README and `.agents` guidance to document checkout wait marker consistency as a general live-report rule, not only a production-scope rule. Future agents are now told that every checkout step's `checkoutWaitCompleted` value in `summary` or `manualEvidence` must match whether the stored redacted checkout command includes `--wait`, and README/package/agent-guidance tests pin that wording. Secret scan, dependency readiness, build, 34 test files, 687 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 02:49 IST after hardening live-report checkout wait consistency. The report validator now rejects checkout summaries and diagnostic manual checkout evidence when `checkoutWaitCompleted` contradicts the sanitized checkout command's `--wait` mode, while production-scope acceptance reuses the same wait-command matcher. Source and installed-package regressions cover forged wait markers in both directions for passing checkout evidence and failed manual checkout evidence. Secret scan, dependency readiness, build, 34 test files, 687 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 02:22 IST after making checkout wait evidence explicit in JSON and live reports. Checkout JSON now includes `checkoutWaitCompleted`; immediate agent JSON reports `false`, explicit wait-mode JSON reports `true` after the human-controlled Enter handoff, sanitized checkout summaries/manual evidence preserve the marker, and production-scope live-report acceptance now requires both the sanitized `--wait` command string and `checkoutWaitCompleted: true` before checkout handoff can support final tracking evidence. Secret scan, dependency readiness, build, 34 test files, 685 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 01:50 IST after making checkout JSON explicit about browser lifecycle. `zepo --visible checkout --json` now reports `browserOpenAfterReturn: false`, immediate JSON guidance points agents to `zepo --visible checkout --wait` or human text checkout when the browser must stay open, and live-verification checkout summaries/manual evidence require the same lifecycle marker without weakening the payment/order automation boundary. Secret scan, dependency readiness, build, 34 test files, 685 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run all passed.
- `npm run check` passed locally on 2026-06-06 at 01:16 IST after broadening checkout manual-boundary detection from the exact `Click to Pay` label to amount-bearing cart-side controls such as `Pay ₹...`, `Pay Now ₹...`, `Tap to Pay ...`, `Proceed to Pay ₹...`, and `Continue to Pay ₹...` while still rejecting payment-method promo text such as `Pay with UPI` and keeping every such control unsafe for automation clicks. Source, README, package-contract, and installed-package regressions now cover the boundary: secret scan, dependency readiness, build, 34 test files, 684 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-06 at 00:50 IST after hardening live verification redaction for assignment-form value options such as `--data-dir=...`, `--phone=...`, `--report=...`, `--browser-locale=...`, and `--browser-timezone=...`; source and installed-package regressions now prove saved live-report command strings and live console command echoes preserve the option name while replacing sensitive values: secret scan, dependency readiness, build, 34 test files, 684 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-06 at 00:30 IST after adding a cart-drawer regression for the current Zepto layout where coupons, out-of-stock items, and alternative product panels appear above the real `Delivering in ...` cart rows and sticky `Click to Pay` amount. The parser now has explicit coverage that it ignores those cart-adjacent panels, reads only active cart rows, and preserves the payable total while checkout still treats amount-bearing payment controls as human-only: secret scan, dependency readiness, build, 34 test files, 683 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-06 at 00:12 IST after aligning saved live-report cart coverage with the runner's non-empty cart JSON contract; edited reports with `cartItemCount: 0` now leave `coverage.cart` false and `missingCoverage.cart` true, and both source and installed-package regressions reject that forged cart evidence outside production-scope mode as well as inside the final readiness gate: secret scan, dependency readiness, build, 34 test files, 682 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 23:52 IST after requiring live-report coverage to satisfy the full runner step contract, including result/exitCode consistency and redacted command shape, before an `ok` workflow step can count as capability coverage. Malformed command/result edits now leave affected capabilities uncovered instead of letting forged step metadata rely only on later report validation: secret scan, dependency readiness, build, 34 test files, 682 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 23:25 IST after requiring live-report coverage summaries to satisfy the runner-known summary schema before a passing step can count as capability coverage, including cart/remove/history-style steps without custom acceptance predicates; local status summaries now write `liveSessionState: "skipped"` explicitly so no-session live reports keep browser preflight and local status coverage accurate under the stricter schema: secret scan, dependency readiness, build, 34 test files, 681 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 22:40 IST after changing live-report coverage summaries to count a known workflow step only when its passing summary also satisfies that step's acceptance contract; edited checkout summaries missing fixed handoff markers now produce `coverage.checkoutHandoff: false` and `missingCoverage.checkoutHandoff: true` instead of overstating checkout handoff readiness: secret scan, dependency readiness, build, 34 test files, 681 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 22:11 IST after tightening live-report checkout handoff acceptance so passing checkout summaries must include fixed handoff markers `handoffUrl: "https://www.zepto.com/?cart=open"` and `handoffSurface: "visible_zepto_browser"`; source and installed-package regressions now reject hand-edited checkout summaries that try to claim checkout handoff coverage without those markers: secret scan, dependency readiness, build, 34 test files, 680 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 21:44 IST after adding source and installed-package regressions that reject checkout JSON/manual live-report evidence missing fixed handoff markers `handoffUrl: "https://www.zepto.com/?cart=open"` and `handoffSurface: "visible_zepto_browser"`; stale checkout evidence now fails with the stable checkout/report contract mismatch codes instead of being treated as live proof: secret scan, dependency readiness, build, 34 test files, 679 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 21:02 IST after adding fixed checkout JSON and live-report handoff markers `handoffUrl: "https://www.zepto.com/?cart=open"` and `handoffSurface: "visible_zepto_browser"` to source output, compiled CLI verification, installed-package verification, sanitized live checkout summaries/manual evidence, README guidance, and agent skills. The markers identify the visible Zepto browser handoff surface only and still do not prove payment/order placement: secret scan, dependency readiness, build, 34 test files, 677 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 20:30 IST after clarifying that sanitized checkout `manualEvidence` is diagnostic only, must preserve `humanActionRequired` plus `automationBoundary`, and cannot be reused as checkout handoff coverage; the installed-package verifier, live-report help, agent guidance, and live verifier tests now assert that boundary: secret scan, dependency readiness, build, 34 test files, 677 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 19:03 IST after carrying checkout `humanActionRequired: true` and `automationBoundary: "zepocli_did_not_click_payment_or_order_controls"` through live-verification checkout summaries, sanitized manual checkout `manualEvidence`, source report acceptance, installed-package report acceptance, and live-verifier guidance: secret scan, dependency readiness, build, 34 test files, 677 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 18:35 IST after adding explicit checkout JSON `humanActionRequired: true` and `automationBoundary: "zepocli_did_not_click_payment_or_order_controls"` fields, changing manual checkout guidance from agent-clickable payment wording to human continuation wording, and raising the subprocess-heavy live-report validator test timeout to 30s for full-suite stability: secret scan, dependency readiness, build, 34 test files, 677 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 17:33 IST after bounding checkout handoff clicks with an explicit timeout and refreshing official Zepto Terms/Privacy source-check dates: secret scan, dependency readiness, build, 34 test files, 676 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 17:00 IST after switching visible Playwright browser contexts to the native maximized window layout while keeping background/headless contexts on the fixed automation viewport: secret scan, dependency readiness, build, 34 test files, 675 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 16:29 IST after hardening live reorder against Zepto's no-id order cards, action-row `Order Again` wrappers, preserved order-card line structure, and bounded order/reorder control clicks, while retaining the cart remove/clear hardening for Zepto's large scrollable cart drawer: secret scan, dependency readiness, build, 34 test files, 675 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 15:37 IST after hardening cart remove/clear against Zepto's large scrollable cart drawer, stale readable cart text without mutation controls, price-only quantity wrappers, and remove-control rerenders during scroll: secret scan, dependency readiness, build, 34 test files, 673 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 14:13 IST after accepting `verify:live:report --max-age-minutes=<minutes>` in addition to the space-separated form and after fixing live address-list selected-state detection for Zepto's ETA-prefixed current-address header: secret scan, dependency readiness, build, 34 test files, 671 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 13:04 IST after making `verify:live` accept value options in both `--option value` and `--option=value` forms while keeping phone input, workflow queries, browser context values, local/report paths, and npm-token-shaped values redacted in live runner diagnostics and report command strings: secret scan, dependency readiness, build, 34 test files, 668 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 12:22 IST after adding explicit `zepo add --remove-limit-items`, hardening cart opening with Zepto's working `/?cart=open` drawer query fallback while still rejecting the broken `/cart` page, bounding cart-open clicks, avoiding unbounded cart scroll scans, tightening hidden empty-cart detection, and allowing high-confidence large-cart reads when Zepto virtualizes a few rows: secret scan, dependency readiness, build, 34 test files, 667 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 08:44 IST after adding explicit `verify:live --cart-remove-limit-items` support and report acceptance for sanitized `zepo --visible cart --remove-limit-items --json` evidence, so the final production-scope live run can resolve Zepto item-limit warnings at the required cart evidence step before checkout when the human explicitly requests the visible `Remove Items` action: secret scan, dependency readiness, build, 34 test files, 664 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 08:13 IST after extending `verify:live --production-scope` with explicit `--checkout-remove-limit-items` support and report acceptance for sanitized `zepo --visible checkout --remove-limit-items --wait --json` evidence, so final live verification can resolve Zepto item-limit warnings only when the human explicitly requests the visible `Remove Items` action: secret scan, dependency readiness, build, 34 test files, 662 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 07:40 IST after adding explicit `zepo --visible checkout --remove-limit-items`, so checkout can handle Zepto's repeated item-limit warning modal with the same bounded `Remove Items` loop as cart before rereading the live cart and continuing the handoff, while plain checkout remains read-only: secret scan, dependency readiness, build, 34 test files, 662 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 07:06 IST after allowing explicit `zepo clear` to resolve Zepto item-limit warnings before clearing the cart, while keeping single-item `zepo remove <query>` from resolving that modal because it could remove unrelated limited items: secret scan, dependency readiness, build, 34 test files, 659 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- Focused `npm test -- tests/cart-automation.test.ts tests/readme.test.ts` and `npm run build` passed locally on 2026-06-05 at 06:59 IST for the same clear/remove item-limit warning boundary: 2 test files, 56 tests.
- `npm run check` passed locally on 2026-06-05 at 06:47 IST after making explicit `zepo cart --remove-limit-items` handle a bounded sequence of repeated Zepto item-limit warning modals before rereading the live cart, while stopping with `cart_limit_exceeded` if Zepto keeps reopening the warning beyond that bounded budget: secret scan, dependency readiness, build, 34 test files, 657 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- Focused `npm test -- tests/cart-automation.test.ts` and `npm run build` passed locally on 2026-06-05 at 06:43 IST after making explicit `zepo cart --remove-limit-items` handle repeated item-limit warning modals and stop with `cart_limit_exceeded` when Zepto keeps reopening them. Plain `zepo cart` remains read-only and still returns `cart_limit_exceeded` when the warning is present.
- `npm run check` passed locally on 2026-06-05 at 06:22 IST after adding explicit `zepo cart --remove-limit-items`, hardening cart-limit modal handling, rejecting partial active-cart parses when Zepto exposes an item count, and documenting the new read-only/default cart boundary: secret scan, dependency readiness, build, 34 test files, 655 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 05:29 IST after adding a regression test that rejects production-scope reports where `track` appears after `checkout_manual_action_required` without checkout handoff coverage: secret scan, dependency readiness, build, 34 test files, 650 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 05:13 IST after making production-scope live verification stop before `track` when checkout remains at `checkout_manual_action_required`: secret scan, dependency readiness, build, 34 test files, 649 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 04:39 IST after hardening checkout manual-action detection to read accessible control labels, including `aria-description` and referenced `aria-labelledby`/`aria-describedby` text: secret scan, dependency readiness, build, 34 test files, 648 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 03:43 IST after making `verify:live` preflight mode-aware so requested live account workflows use visible doctor/status readiness while no-account/local smokes keep background preflight: secret scan, dependency readiness, build, 34 test files, 647 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 03:05 IST after adding mode-specific browser automation readiness diagnostics for background/headless versus visible human-controlled flows: secret scan, dependency readiness, build, 34 test files, 646 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 02:27 IST after hardening Zepto cart/add/checkout recovery, navigation-timeout errors, non-empty cart live-report contracts, and installed-package report fixtures: secret scan, dependency readiness, build, 34 test files, 645 tests, compiled CLI smoke, installed-package smoke, help output, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-05 at 00:12 IST after hardening checkout/cart recovery for Zepto cart surfaces that stay unverified or unreadable after the first cart open: secret scan, dependency readiness, build, 34 test files, 641 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-04 at 23:25 IST after hardening cart opening for Zepto header cart controls that render as exact non-semantic `Cart`/`Cart <count>` labels while still avoiding any direct `/cart` navigation: secret scan, dependency readiness, build, 34 test files, 638 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-04 at 22:57 IST after adding sanitized checkout `manualEvidence` to incomplete live reports: secret scan, dependency readiness, build, 34 test files, 637 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-04 at 22:27 IST after making the final `--production-scope` live-verification preset enable checkout wait automatically: secret scan, dependency readiness, build, 34 test files, 636 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-04 at 21:59 IST after requiring explicit checkout wait-mode evidence for final production-scope report acceptance: secret scan, dependency readiness, build, 34 test files, 636 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-04 at 21:18 IST after adding checkout wait-mode handoff refresh: secret scan, dependency readiness, build, 34 test files, 636 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-04 at 20:54 IST after adding explicit `--no-input` coverage for `zepo checkout --json --wait`: secret scan, dependency readiness, build, 34 test files, 633 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-04 at 20:18 IST after adding explicit checkout wait support: secret scan, dependency readiness, build, 34 test files, 633 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally on 2026-06-04 at 19:40 IST after the cart-read recovery and swap-suggestion parser fixes: secret scan, dependency readiness, build, 34 test files, 631 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- `npm run check` passed locally again on 2026-06-04 at 18:02 IST after the order-history navigation hardening: 34 test files, 628 tests, compiled CLI smoke, installed-package smoke, audit, pack dry-run, and publish dry-run.
- A fresh compiled CLI smoke on 2026-06-05 with `node dist/index.js --data-dir ./.zepo-mode-smoke status --json`, `node dist/index.js --data-dir ./.zepo-mode-smoke --visible status --json`, and `node dist/index.js --data-dir ./.zepo-mode-smoke doctor --skip-browser --json` reported `browserAutomation.modes.backgroundHeadless` and `browserAutomation.modes.visibleHumanControlled` readiness details. Background status reported `browserAutomationMode.current: "background_headless"`; visible status reported `browserAutomationMode.current: "visible_human_controlled"`. Both modes were ready in the disposable no-session smoke, with no browser lock, no profile data, no headless run history, and no access cooldown.
- A fresh compiled CLI smoke on 2026-06-04 with `node dist/index.js --data-dir ./.zepo-final-smoke status --json` reported `browserAutomationMode.default: "background_headless"`, `browserAutomationMode.current: "background_headless"`, `browserAutomationMode.visibleRequested: false`, `browserAutomation.ready: true`, no browser lock, no profile data, no headless run history, and no access cooldown.
- The npm package shape, compiled CLI entry, installed `zepo` binary, command help, JSON error contracts, no-surprise-browser guards, dependency readiness, secret scanning, audit, pack dry-run, and publish dry-run were verified by the local gate.
- `zepo completion <bash|zsh|fish|powershell>` is now part of the developer CLI surface. It is generated from the registered command tree plus Commander built-ins, includes `help`, nested help topics such as `help address`, and built-in `-h/--help`, and remains runtime-free and browser-free; compiled and installed-package smokes cover bash, zsh, fish, PowerShell, the `pwsh`/`ps1` aliases, and unsupported-shell JSON errors, and the installed README documents those PowerShell aliases.
- Compiled and installed-package verifiers also assert that `zepo --data-dir <new-dir> completion bash` does not create the requested data directory, proving shell completion generation does not initialize runtime storage, services, SQLite, or Playwright.
- The packed npm package now ships the exposed verifier entrypoints `scripts/verify-cli.mjs`, `scripts/verify-package.mjs`, `scripts/verify-live-flow.mjs`, and `scripts/verify-live-report.mjs`; installed-package checks assert `verify:live` and `verify:live:report` are wired to those scripts, and disposable installed-package `npm run verify:cli --silent` and `npm run verify:package --silent` smokes passed on 2026-06-04. Installed-package `verify:package` uses `npm pack --ignore-scripts` for its nested pack so runtime installs do not need dev-only `tsc`.
- Installed-package commands default to background/headless browser automation. Human-only login, address-add, and checkout handoffs require `--visible` and fail early with `visible_browser_required` before browser launch/profile writes/headless run accounting.
- `zepo --visible checkout --json` still returns structured checkout evidence immediately for agents. Callers can now pass `--wait` explicitly when a human wants the visible Zepto checkout/payment browser to stay open before JSON returns; `verify:live --checkout-wait` passes that option and may continue to `track` after the manual Zepto boundary, but manual payment controls still do not count as checkout handoff coverage.
- Explicit checkout wait mode now re-checks the visible Zepto page after the human presses Enter. If a human-clicked manual continuation reaches a real checkout/payment handoff surface, JSON can return `checkout_handoff_returned`; otherwise it keeps `checkout_manual_action_required` as incomplete manual-continuation evidence.
- `zepo --no-input checkout --json --wait` is now covered by compiled and installed-package checks and fails with `interactive_input_required` before browser work, preserving unattended automation safety for the explicit wait mode.
- Final CLI help now describes `login`, `address add`, and `checkout` as commands that require `--visible`, while the global `--visible` option documents that the default remains background/headless. After that wording change, `npm run check` passed on 2026-06-04.
- Do not parallelize browser-capable commands against the same `--data-dir`, including quick status/search/login/checkout experiments. ZepoCli intentionally serializes browser work per data directory and will report `browser_lock_active` on overlapping runs; run such checks serially or use separate data directories only for independent sessions.

## External Zepto References

- Official Zepto Terms of Use were rechecked on 2026-06-05 at `https://www.zepto.com/s/terms-of-service`: version 1.4, last updated 1 November 2025.
- Official Zepto Privacy Notice was rechecked on 2026-06-05 at `https://staticweb.zepto.com/privacy-policy/`: version 1.1, last updated 17 June 2025.
- Keep the CLI aligned with these boundaries: user-directed access only, no resale/bulk/load-generation flows, no payment handling inside the CLI, no OTP/payment credential capture, and no bypassing Zepto protections.

## Live Zepto Evidence

- A fresh `.zepo-live-prod` production-scope attempt on 2026-06-07 at 15:47 IST used `npm --silent run verify:live -- --data-dir ./.zepo-live-prod --login --production-scope --search milk --address home --add "Amul Milk 500ml" --add-remove-limit-items --cart-remove-limit-items --checkout-remove-limit-items --step-timeout 300000`. It passed visible doctor, visible local status, visible live-session, address selection, search, add, and non-empty cart with total coverage. Checkout ran as `--visible checkout --remove-limit-items --wait --json` and failed with `live_command_timeout` at the human-controlled Zepto checkout prompt, so `verify:live:report --require-production-scope --max-age-minutes 1440 ./.zepo-live-prod/live-verification-report.json` correctly rejected the report for missing checkout handoff and track coverage. This is current live progress only, not final production readiness. Post-run `zepo status --json` showed a stale browser lock with a non-running owner process; a follow-up visible `doctor --json` still passed browser automation and Playwright Chromium while reporting the stale lock as a warning. Agents should not manually delete active-looking locks.
- A focused `.zepo-live-prod` checkout-boundary report on 2026-06-06 at 11:33 IST using `./.zepo-live-prod/live-checkout-boundary-current-report.json` passed visible doctor, local status, and visible live-session checks, then failed checkout with `live_verification_incomplete` because Zepto exposed a human-only amount-bearing payment-control continuation. The sanitized report shows diagnostic `coverage.checkoutManualBoundary: true`, `manualPaymentControlVisible: true`, `checkoutCartItemCount: 3`, `checkoutHasPayableTotal: true`, `cartPrecondition: "non_empty_cart_verified"`, `paymentStatus: "not_observed_by_zepocli"`, `orderPlacement: "not_confirmed_by_zepocli"`, and `orderStatusCommand: "zepo track"`. The validator correctly rejects it as an acceptable checkout report because requested `coverage.checkoutHandoff` remains false; this is current manual-boundary evidence only, not production-scope readiness.
- A current no-mutation `.zepo-live-prod` live-runner preflight on 2026-06-06 at 11:29 IST using `./.zepo-live-prod/live-local-preflight-current-report.json` passed background `doctor --json` and local `status --json`. `npm --silent run verify:live:report -- --max-age-minutes 60 ./.zepo-live-prod/live-local-preflight-current-report.json` accepted the saved sanitized report. It proves only browser preflight plus local confirmed-session status for the production data directory; it intentionally did not request or cover live session, address selection, search, add, cart, checkout handoff, track, history, or reorder.
- `./.zepo-live-prod/live-production-current-report.json` was rechecked on 2026-06-06 at 11:27 IST with `npm --silent run verify:live:report -- --require-production-scope --max-age-minutes 1440 ./.zepo-live-prod/live-production-current-report.json`; the current validator still rejected it. The saved report is `ok: false`, generated on 2026-06-05, and shows requested production-scope coverage for browser preflight, local status, live session, address selection, search, add, cart, checkout handoff, and track. Browser preflight, live session, address selection, search, add, and cart were covered, but checkout timed out at the human-controlled `zepo --visible checkout --remove-limit-items --wait --json` step, so `coverage.checkoutHandoff` and `coverage.track` remain false and `missingCoverage.checkoutHandoff` plus `missingCoverage.track` remain true. This is current rejection evidence, not final readiness proof.
- The previous focused checkout-boundary report `./.zepo-live-prod/live-checkout-accessible-boundary-report.json` was rechecked on 2026-06-05 at 20:08 IST with `npm --silent run verify:live:report -- --max-age-minutes 1440 ./.zepo-live-prod/live-checkout-accessible-boundary-report.json`. The current validator rejected it with `live_report_not_ok`, `live_report_step_contract_mismatch`, missing checkout handoff coverage, and missing coverage still true. This is expected because manual checkout evidence is diagnostic only and current manual evidence must preserve `humanActionRequired` plus `automationBoundary`; old saved checkout-boundary reports remain stale/incomplete and must not be reused as final readiness proof.
- Direct `.zepo-live-prod` visible checkout verification on 2026-06-05 after switching visible Playwright contexts to the native maximized window layout returned `checkout_manual_action_required` with `cartPrecondition: "non_empty_cart_verified"`, `paymentStatus: "not_observed_by_zepocli"`, `orderPlacement: "not_confirmed_by_zepocli"`, and `orderStatusCommand: "zepo track"`, with no browser lock left behind. A human-provided screenshot of that Playwright window showed Zepto's sticky cart-side `Click to Pay` footer present, confirming the previous missing-footer issue was a visible viewport/layout mismatch rather than missing cart state. This remains manual Zepto continuation evidence only; ZepoCli still does not click the payment control or count it as production-scope checkout handoff coverage.
- A fresh `.zepo-live-prod` production-scope run on 2026-06-05 using `./.zepo-live-prod/live-production-current-report.json` passed visible doctor, local status, visible live-session, saved-address selection, visible search, visible add with `--remove-limit-items`, and visible cart with `--remove-limit-items`. It reached `zepo --visible checkout --remove-limit-items --wait --json` and then timed out waiting for the human-controlled Zepto payment/checkout continuation, so checkout handoff and track coverage remain missing. `npm --silent run verify:live:report -- --require-production-scope --max-age-minutes 1440 ./.zepo-live-prod/live-production-current-report.json` correctly rejected the report with missing checkoutHandoff and track coverage. A follow-up visible `status --live --json` recovered the stale lock left by the timed-out checkout child and confirmed no browser lock remains.
- A focused `.zepo-live-prod` reorder report on 2026-06-05 using `./.zepo-live-prod/live-reorder-current-report.json` passed visible doctor, local status, visible live-session, visible `reorder last --json`, and visible `cart --remove-limit-items --json` after the order-card wrapper and bounded-click hardening. `npm --silent run verify:live:report -- --max-age-minutes=60 ./.zepo-live-prod/live-reorder-current-report.json` accepted the saved sanitized report. This proves current focused reorder coverage only; it intentionally may add latest-order items back to the Zepto cart and is not final production-scope proof.
- A focused `.zepo-live-prod` cart-remove report on 2026-06-05 using `./.zepo-live-prod/live-remove-current-report.json` passed visible doctor, local status, visible live-session, visible `remove --json`, and visible `cart --remove-limit-items --json` after the cart remove hardening. `npm --silent run verify:live:report -- --max-age-minutes=60 ./.zepo-live-prod/live-remove-current-report.json` accepted the saved sanitized report. This proves current focused remove coverage on the live cart only; it intentionally mutated the Zepto cart and is not final production-scope proof.
- Direct `.zepo-live-prod` visible CLI verification on 2026-06-05 at 14:15 IST confirmed the saved session with `status --live --json`, returned real visible search results for `milk`, added `Country Delight Cow Fresh Milk` with `add --remove-limit-items --json`, reread a 33-item live cart with total `₹2120`, marked the current saved address selected in `address list --json`, returned current `history --json` and `track --json` summaries, and stopped checkout at `checkout_manual_action_required` with `cartPrecondition: "non_empty_cart_verified"`, `paymentStatus: "not_observed_by_zepocli"`, `orderPlacement: "not_confirmed_by_zepocli"`, and `orderStatusCommand: "zepo track"`. A background/headless `search milk --json` attempt on the same profile stopped with `error.code: "zepto_access_challenge"` and `retryAfterMs: 900000`; this confirms the CLI stops instead of bypassing Zepto protection and that visible human-controlled mode is the current safe live path. This is direct live command evidence, not an accepted production-scope report.
- Direct `.zepo-live-prod` visible debugging on 2026-06-05 used live screenshots of the Playwright Chromium window while `zepo --visible add "amul butter" --remove-limit-items --json` was running. The screenshot showed Zepto parked on a scrolled homepage/search surface with the cart badge updated but no open cart drawer, proving the slow path was the post-add cart verification opener, not the product ADD click. After adding the `/?cart=open` drawer fallback, two visible add probes completed cleanly: the first verification read returned 32 readable cart items with a total, and the second command emitted JSON with a selected product, 36 readable cart items, a total, no stderr, and no browser lock left behind. No screenshot files were added to the repository.
- A fresh disposable no-account `verify:live` smoke on 2026-06-05 using `./.zepo-clear-boundary-smoke` passed `doctor --json` with Playwright Chromium launch evidence and local `status --json`, then stopped at the manual session precondition with stable `live_verification_incomplete`. The saved sanitized report showed browser preflight and local status coverage only, with no login, live-session, search, cart, clear, checkout, track, history, or reorder workflow attempted. `npm --silent run verify:live:report -- ./.zepo-clear-boundary-smoke/live-verification-report.json` correctly rejected the report with `live_report_not_ok`, so this is current live-runner preflight evidence only and not production-scope acceptance evidence.
- A safe no-account `verify:live` smoke on 2026-06-04 using `./.zepo-live-smoke-current` passed `doctor` and local `status`, then stopped at the manual session precondition with `live_verification_incomplete`; it did not claim login, live-session, checkout, or order coverage. The ordinary report validator rejected the saved report with `live_report_not_ok`, which is correct because this smoke is incomplete evidence, not an acceptable live pass.
- A disposable visible-preflight `verify:live` smoke on 2026-06-05 using `./.zepo-visible-preflight-smoke` and a requested search workflow passed `--visible doctor --json` and `--visible status --json`, then stopped at the missing-session precondition with `live_verification_incomplete`. This confirms requested live workflows no longer fail browser preflight solely because background/headless is cooling down; it did not touch search/cart/checkout and is not acceptance evidence.
- A focused no-mutation `.zepo-live-prod` live-session report on 2026-06-05 using `./.zepo-live-prod/live-session-visible-preflight-report.json` passed `--visible doctor --json`, `--visible status --json`, and visible `status --live --json`; `npm --silent run verify:live:report -- --max-age-minutes 60 ./.zepo-live-prod/live-session-visible-preflight-report.json` accepted the saved report. This proves the new mode-aware preflight works for an existing confirmed session without changing cart or checkout state; it is focused live-session evidence only, not final production-scope proof.
- A focused no-mutation `.zepo-live-prod` cart report on 2026-06-05 using `./.zepo-live-prod/live-cart-visible-preflight-report.json` passed `--visible doctor --json`, `--visible status --json`, visible `status --live --json`, and visible `cart --json`; `npm --silent run verify:live:report -- --max-age-minutes 60 ./.zepo-live-prod/live-cart-visible-preflight-report.json` accepted the saved report. The sanitized report summary showed `liveSessionState: "logged-in"`, `cartItemCount: 35`, and `hasTotal: true`, without storing cart item names or raw page text. This proves current focused cart coverage through the live runner only; it is not final production-scope proof.
- A focused `.zepo-live-prod` checkout-boundary report on 2026-06-05 at 04:51 IST using `./.zepo-live-prod/live-checkout-accessible-boundary-report.json` passed visible preflight and visible live-session checks with the latest checkout accessible-label detection, then stopped at checkout with `live_verification_incomplete`. The sanitized manual evidence preserved `status: "checkout_manual_action_required"`, `cartPrecondition: "non_empty_cart_verified"`, `paymentStatus: "not_observed_by_zepocli"`, `orderPlacement: "not_confirmed_by_zepocli"`, and `orderStatusCommand: "zepo track"`. `npm --silent run verify:live:report -- --max-age-minutes 60 ./.zepo-live-prod/live-checkout-accessible-boundary-report.json` correctly rejected the saved report for missing checkout handoff coverage, so this is current checkout-boundary evidence only and not final production-scope proof.
- The same `./.zepo-live-prod/live-checkout-accessible-boundary-report.json` report was rechecked on 2026-06-05 at 05:24 IST with `npm --silent run verify:live:report -- --require-production-scope --max-age-minutes 1440`. The production-scope gate correctly rejected it with missing checkout handoff coverage and missing production workflow coverage, confirming manual checkout-boundary evidence plus focused live-session/cart proof still cannot be treated as final readiness.
- A focused `.zepo-live-prod` checkout-boundary report on 2026-06-05 using `./.zepo-live-prod/live-checkout-boundary-report.json` passed visible preflight and visible live-session checks, then stopped at checkout with `live_verification_incomplete` because Zepto exposed `checkout_manual_action_required`. The sanitized manual evidence preserved `cartPrecondition: "non_empty_cart_verified"`, `paymentStatus: "not_observed_by_zepocli"`, `orderPlacement: "not_confirmed_by_zepocli"`, and `orderStatusCommand: "zepo track"`. `npm --silent run verify:live:report -- --max-age-minutes 60 ./.zepo-live-prod/live-checkout-boundary-report.json` correctly rejected the saved report for missing checkout handoff coverage, proving ZepoCli still refuses to treat a cart-side manual payment control as payment, order placement, or production-scope checkout handoff evidence.
- A disposable no-account checkout-wait smoke on 2026-06-04 using `./.zepo-checkout-wait-smoke` ran `verify:live --checkout --checkout-wait`, passed `doctor` and local `status`, then stopped at the manual session precondition with `live_verification_incomplete`. It did not launch checkout, did not claim live-session/checkout coverage, and the ordinary report validator rejected the saved report as incomplete.
- A disposable no-account checkout-refresh smoke on 2026-06-04 using `./.zepo-refresh-smoke` ran `verify:live --checkout --checkout-wait`, passed `doctor` and local `status`, then stopped at the manual session precondition with `live_verification_incomplete`. The saved report was rejected as incomplete, confirming the refreshed wait-mode code does not bypass the login/live-session precondition.
- A disposable no-account production-scope wait smoke on 2026-06-04 using `./.zepo-wait-required-smoke` ran the final command shape with `--production-scope --checkout-wait`, passed `doctor` and local `status`, then stopped at the manual session precondition with `live_verification_incomplete`. The saved report was rejected by `verify:live:report --require-production-scope --max-age-minutes 60` because live session, search, address selection, add, cart, checkout handoff, and track coverage were all missing, confirming the final gate still does not bypass login or checkout preconditions.
- A disposable no-account production-scope preset smoke on 2026-06-04 using `./.zepo-production-preset-smoke` ran the simplified final command shape with `--production-scope` and no separate `--checkout-wait`, passed `doctor` and local `status`, then stopped at the manual session precondition with `live_verification_incomplete`. The saved report was rejected by `verify:live:report --require-production-scope --max-age-minutes 60` for missing live session/search/address/add/cart/checkout/track coverage, confirming the preset does not bypass account or checkout proof.
- A conservative public headless search smoke on 2026-06-04 returned Zepto HTTP 429 and the CLI emitted structured `error.code: "zepto_access_challenge"` with `retryAfterMs: 900000`.
- A `.zepo-live-prod` headless `cart --json` probe on 2026-06-04 stopped on Zepto HTTP 429 from the Zepto home page and emitted structured `error.code: "zepto_access_challenge"` with `retryAfterMs: 900000`; no direct `/cart` route was used.
- A `.zepo-live-prod` local-status check on 2026-06-04 showed a confirmed local session marker with no active browser lock. Earlier headless `status --live --json` checks returned Zepto HTTP 429 with `error.code: "zepto_access_challenge"` and `retryAfterMs: 900000`, including one retry after the cooldown cleared; do not loop headless live-session checks after this signal.
- A focused human-controlled visible live-session probe on 2026-06-04 using `.zepo-live-prod` and `npm --silent run verify:live -- --data-dir ./.zepo-live-prod --login --report ./.zepo-live-prod/live-session-report.json --step-timeout 120000` passed `doctor`, local `status`, and visible `status --live --json`. The saved focused report was accepted by `npm --silent run verify:live:report -- --max-age-minutes 1440 ./.zepo-live-prod/live-session-report.json`. This proves fresh browser preflight, local status, and live-session coverage only; it does not prove address selection, search, add, cart, checkout handoff, or track.
- Focused `.zepo-live-prod` reports for `search`, `address list`, `address use`, `add` plus non-empty `cart`, `history`, and `track` were revalidated on 2026-06-05 with `npm --silent run verify:live:report -- --max-age-minutes 1440 <report>`, and each was accepted. This proves those focused workflow contracts still have fresh saved evidence, but focused reports cannot be combined as final production-scope proof.
- A focused human-controlled visible order-history probe on 2026-06-04 using `.zepo-live-prod` and `npm --silent run verify:live -- --data-dir ./.zepo-live-prod --login --history --report ./.zepo-live-prod/live-history-report.json --step-timeout 180000` passed `doctor`, local `status`, visible `status --live --json`, and visible `history --json` after adding a bounded account-surface settle and `/account` fallback that still avoids direct `/orders`. The saved report was accepted by `npm --silent run verify:live:report -- --max-age-minutes 1440 ./.zepo-live-prod/live-history-report.json`. This proves focused history coverage only; it does not prove checkout handoff or post-payment order tracking.
- A focused visible add/cart probe on 2026-06-04 using `.zepo-live-prod` and a current exact product query passed `zepo add --json` after the bounded cart-read recovery. It proved the add command can recover when Zepto opens an unhydrated or initially unconfirmed cart surface, while still avoiding any direct `/cart` URL.
- A sanitized visible cart probe after the swap-suggestion parser fix reported a non-empty cart with a total and zero cart rows named as Zepto swap controls. This proves the cart parser no longer counts Zepto's swap suggestion controls as active cart items in that live session.
- The latest `.zepo-live-prod/live-verification-report.json` production-scope run on 2026-06-04 passed browser preflight, local status, live session, address selection, search, add, and cart coverage. It failed at checkout with `live_verification_incomplete` because Zepto exposed a manual payment-control action that ZepoCli correctly does not click. The saved report was checked with `verify:live:report --require-production-scope --max-age-minutes 1440` and was rejected: checkout handoff coverage did not pass, track coverage did not pass, and production-scope coverage is missing.
- A `.zepo-live-prod` production-scope attempt on 2026-06-04 passed browser preflight, local status, live session, address selection, search, add, and cart, then exposed a checkout cart-precondition failure when checkout parsed only the first visible cart shell. Checkout now reuses the recovered `readCart` path instead of a separate direct visible-cart parse.
- A direct `.zepo-live-prod` visible checkout-wait probe after that fix returned structured `checkout_manual_action_required` with `cartPrecondition: "non_empty_cart_verified"`, `paymentStatus: "not_observed_by_zepocli"`, and `orderPlacement: "not_confirmed_by_zepocli"`. This proves the checkout command verifies a non-empty cart before the safe manual Zepto boundary, but it is still not checkout handoff coverage.
- A later `.zepo-live-prod` production-scope attempt exposed a separate `cart_unreadable` after add even though add had reported a non-empty cart. `readCart` now performs one additional bounded recovery for repeated unhydrated cart shells, still without using a direct `/cart` URL.
- A direct `.zepo-live-prod` visible cart probe after the bounded recovery fix read a non-empty live cart with 11 readable items and a total while still avoiding direct `/cart` navigation.
- Direct `.zepo-live-prod` visible cart debugging on 2026-06-05 exposed Zepto's item-limit modal (`You've exceeded limit for these items for today`) and cart-adjacent out-of-stock/alternative product panels. ZepoCli now treats the warning as `cart_limit_exceeded` for plain `zepo cart`, keeps that command read-only, and requires explicit `zepo cart --remove-limit-items` before clicking Zepto's visible `Remove Items` action. Cart parsing now requires the active cart rows to match Zepto's visible `N items` header exactly, so partial drawer parses are rejected instead of returned as cart evidence.
- A direct `.zepo-live-prod` background `cart --remove-limit-items --json` probe on 2026-06-05 stopped on Zepto HTTP 429 with stable `error.code: "zepto_access_challenge"` and `retryAfterMs: 900000`; no checkout/payment action ran. Visible retry attempts around the same time exceeded the external shell timeout while Zepto was in the challenged/cooling-down state, so they are not acceptance evidence and should not be looped.
- A direct `.zepo-live-prod` background `cart --json` probe on 2026-06-05 after cooldown cleared still stopped on Zepto HTTP 429 with stable `error.code: "zepto_access_challenge"` and `retryAfterMs: 900000`. This confirms background/headless is currently blocked by Zepto for the live account state; do not retry headless in a loop.
- A direct `.zepo-live-prod` visible `cart --json` probe on 2026-06-05 succeeded without using `/cart`, returning 34 readable live cart items and total `₹1821`.
- A direct `.zepo-live-prod` visible `checkout --json` probe on 2026-06-05 returned `checkout_manual_action_required` with `cartPrecondition: "non_empty_cart_verified"`, `paymentStatus: "not_observed_by_zepocli"`, `orderPlacement: "not_confirmed_by_zepocli"`, and `orderStatusCommand: "zepo track"`. This is correct safety behavior and still does not count as production-scope checkout handoff coverage.
- A direct `.zepo-live-prod` visible `add "Colgate toothpaste" --json` probe on 2026-06-05 selected `Colgate Strong Teeth Anticavity Toothpaste`, added it, and returned a live cart containing that item with updated total `₹1924`.
- A focused `.zepo-live-prod` live runner attempt on 2026-06-05 for address/search/add/cart stopped at doctor preflight with `live_doctor_contract_mismatch` because the previous headless 429 placed the data directory in `zepto_access_cooldown`. The report is correctly incomplete and not acceptance evidence.
- A focused `.zepo-live-prod` checkout/track run on 2026-06-04 passed browser preflight, local status, visible live session, and `track`. Checkout stopped at `checkout_manual_action_required` with sanitized manual evidence, and the ordinary report validator correctly rejected the report with `live_report_not_ok`/missing checkout handoff coverage because no Zepto-side payment-control action was completed by a human.
- The saved `.zepo-live-prod/live-production-current-report.json` was rechecked on 2026-06-05 with `verify:live:report --require-production-scope --max-age-minutes 1440` and remains rejected. It is not current final evidence because the saved run predates the latest bounded cart recovery and is missing cart, checkout handoff, and track coverage.
- Do not loop headless Zepto commands after this signal. Wait for cooldown or use an explicitly human-controlled visible flow when the user asks for it.
- This 429 result is not a product-search failure to work around with stealth, custom user agents, CAPTCHA bypasses, or aggressive retries. It is correct stop behavior under the project safety rules.

## Still Required Before Claiming Production Ready

- A fresh human-controlled production-scope live report is still missing.
- Required final command shape:

```bash
npm --silent run verify:live -- --data-dir <dedicated-dir> --login --production-scope --search <query> --address <query> --add <query>
```

- Required acceptance command:

```bash
npm --silent run verify:live:report -- --require-production-scope --max-age-minutes 1440 <report-path>
```

- Final readiness requires passing coverage for browser preflight, local status, live session, address selection, search, add, non-empty cart with total/payable evidence, checkout handoff, and track, with checkout evidence from explicit wait mode before tracking. Checkout handoff is not payment proof; payment and final order placement remain Zepto-side and unobserved by ZepoCli.
